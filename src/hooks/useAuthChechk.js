import React from "react"
import axios from "../AxiosInstance/axios-auth"
import { useSelector } from "react-redux"

const useAuthChechk = async () => {

    const
    const res = await axios.post("accounts:lookup",{

    })
}